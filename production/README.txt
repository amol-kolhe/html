
The index.html file provided in this directory (production) is designed to point to the minified scripts and the styles.
So on the production setup one has to copy the index.html file from the production directory to root level (Usually /usr/share/nginx/html).
Such that the html directory will contain index.html and the web directory.
